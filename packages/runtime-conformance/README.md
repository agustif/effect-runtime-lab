# @effect-experimental/runtime-conformance

Shared conformance and contract definitions for runtime candidates.

This package exists so runtime tracks can be compared against the same rules instead of inventing their own test stories.
