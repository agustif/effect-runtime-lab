# Unsupported and deferred

## Explicitly deferred until new evidence exists
- Linux and macOS deployability guarantees beyond current local build evidence
- full filesystem parity across all edge cases
- temp primitive parity at production depth
- full file-handle parity
- the remaining redirect, abort, and cancellation parity matrix
- REPL, AI, and extension layers in this package

## Explicit non-goals
- v3 compatibility
- treating the package as feature-complete because the current prod lane passes
- collapsing txiki-specific behavior into this package
