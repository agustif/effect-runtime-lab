# Unsupported and deferred

## Explicitly deferred until new evidence exists
- executable and dependency guarantees from this repo
- full filesystem parity across all edge cases
- full file-handle parity
- the remaining redirect, abort, and cancellation parity matrix
- REPL, AI, and extension layers in this package

## Explicit non-goals
- v3 compatibility
- treating txiki as the same runtime substrate as the custom QuickJS host
- marking partial filesystem or HTTP behavior as fully verified before the shared conformance suite covers it
