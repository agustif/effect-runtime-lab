# @effect-experimental/runtime-txiki

This package is the dedicated txiki.js exploration track.

Why it exists:
- keep the richer-runtime evaluation separate from the tiny QuickJS-core track
- avoid contaminating the QuickJS core package with txiki-specific assumptions
- make runtime comparisons explicit and measurable
